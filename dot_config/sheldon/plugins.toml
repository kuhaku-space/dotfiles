
shell = "zsh"

apply = ["defer"]

[templates]
defer = "{{ hooks?.pre | nl }}{% for file in files %}zsh-defer source \"{{ file }}\"\n{% endfor %}{{ hooks?.post | nl }}"

[plugins]

[plugins.base16]
github = "chriskempson/base16-shell"
apply = ["source"]

[plugins.zsh-defer]
github = "romkatv/zsh-defer"
apply = ["source"]

[plugins.zsh-completions]
github = 'zsh-users/zsh-completions'
apply = ["source"]

[plugins.compinit]
inline = '''
_init_compinit() {
  local zdump="${XDG_CACHE_HOME:-$HOME/.cache}/zsh/zcompdump-$ZSH_VERSION"
  autoload -Uz compinit
  compinit -d "$zdump"
  [[ -s "$zdump.zwc" && "$zdump.zwc" -nt "$zdump" ]] || zcompile "$zdump"
  local carapace_init="${XDG_CACHE_HOME:-$HOME/.cache}/zsh/carapace-init.zsh"
  [[ -s $carapace_init ]] && source "$carapace_init"
  unset -f _init_compinit
}
zsh-defer _init_compinit
'''

[plugins.zsh-autosuggestions]
github = "zsh-users/zsh-autosuggestions"

[plugins.fast-syntax-highlighting]
github = "zdharma-continuum/fast-syntax-highlighting"

[plugins.zeno]
github = "yuki-yano/zeno.zsh"

[plugins.atuin]
inline = '''
_atuin_cache="${XDG_CACHE_HOME:-$HOME/.cache}/zsh/atuin-init.zsh"
if [[ ! -s $_atuin_cache ]] && (( $+commands[atuin] )); then
  mkdir -p "${_atuin_cache:h}"
  atuin init zsh --disable-up-arrow --disable-ai >| $_atuin_cache
fi
[[ -s $_atuin_cache ]] && source $_atuin_cache
unset _atuin_cache
'''

[plugins.starship]
inline = '''
_starship_cache="${XDG_CACHE_HOME:-$HOME/.cache}/zsh/starship-init.zsh"
if [[ ! -s $_starship_cache ]] && (( $+commands[starship] )); then
  starship init zsh >| $_starship_cache
fi
[[ -s $_starship_cache ]] && source $_starship_cache
unset _starship_cache
'''

[plugins.zoxide]
inline = '''
_init_zoxide() { eval "$(zoxide init zsh)"; unset -f _init_zoxide }
zsh-defer _init_zoxide
'''
